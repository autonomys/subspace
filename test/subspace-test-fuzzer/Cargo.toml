[package]
name = "subspace-test-fuzzer"
version = "0.1.0"
edition.workspace = true
authors = ["Aarnav Bos <aarnav@srlabs.de>"]
license = "0BSD"
homepage = "https://subspace.network"
repository = "https://github.com/autonomys/subspace"
description = "Fuzzing harness for Subspace"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
pallet-domains = { workspace = true, default-features = false, features = ["fuzz"] }
ziggy.workspace = true
